---
"suncode": major
---

Move global user instructions from `~/.agents.md` to the SunCode-specific `~/.suncode/AGENTS.md`. The old path is no longer loaded. Merge any existing rules into the new file before upgrading, preserving rules already present there. Project instruction loading is unchanged.
